[project]
name = "soc-copilot"
version = "0.1.0"
description = "AI-assisted triage for authentication anomalies (Type-2 integrated project)."
authors = [{ name = "Stav Hefetz" }, { name = "Bar Koldanov" }]
requires-python = ">=3.11"

dependencies = [
    "ag2[openai]>=0.5",
    "chainlit>=1.1",
    "scikit-learn>=1.4",
    "pandas>=2.2",
    "numpy>=1.26",
    "joblib>=1.3",
    "matplotlib>=3.8",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
